export default function Footer() {
  return (
    <footer className="absolute bottom-0 left-0 w-full py-3 text-xs italic text-center text-gray-500 text-xs">
      Copyright © {new Date().getFullYear()} @Trap.io
    </footer>
  );
}
